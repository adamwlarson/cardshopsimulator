# Staff meshes (optional cashier)

| Asset | Height | Clip | Notes |
|-------|--------|------|-------|
| `char_cashier_01` | 1.72 m | `idle_stand` ~2.0s in-place | FOV polish: warm canvas apron + blank name-tag |

Pivot: bottom-center. Principled-only. `StaffPresenter` instances this behind the register when a cashier is on duty. See `IMPORT_NOTES.md`. Specialist is domain-only for MVP (no floor mesh). Soft teal polo parked — apron carries cast.
